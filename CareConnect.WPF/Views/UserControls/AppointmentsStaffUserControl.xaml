<UserControl x:Class="CareConnect.WPF.Views.UserControls.AppointmentsStaffUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CareConnect.WPF.Views.UserControls"
             mc:Ignorable="d" 
             xmlns:controlsviewmodels="clr-namespace:CareConnect.WPF.ViewModels.UserControls"
             d:DataContext="{d:DesignInstance Type=controlsviewmodels:AppointmentsClientViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F4F6F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="1" SelectedValuePath="Content" SelectedValue="{Binding SelectedPeriodAppointment}" HorizontalAlignment="Stretch" Margin="150 0 20 0">
                <ComboBoxItem Content="All appointments"/>
                <ComboBoxItem Content="Future appointments"/>
                <ComboBoxItem Content="Past appointments"/>
            </ComboBox>
        </Grid>
        <DataGrid Grid.Column="0" Grid.Row="1" ItemsSource="{Binding AppointmentsList}" Margin="0 0 20 0" AutoGenerateColumns="False" CanUserAddRows="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding BookingDate, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" Width="*"/>
                <DataGridTextColumn Header="Service" Binding="{Binding IdServiceNavigation.Name}" Width="*"/>
                <DataGridTextColumn Header="First name" Binding="{Binding IdUserNavigation.FirstName}" Width="*"/>
                <DataGridTextColumn Header="Last name" Binding="{Binding IdUserNavigation.LastName}" Width="*"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding IdServiceNavigation.EstTimeMinutes, StringFormat='{}{0} minutes'}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding IdServiceNavigation.Price, StringFormat='{}{0} lei'}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
