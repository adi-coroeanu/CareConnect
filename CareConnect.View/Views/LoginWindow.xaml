<Window x:Class="CareConnect.View.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CareConnect.View.Views"
        mc:Ignorable="d"
        xmlns:viewmodels="clr-namespace:CareConnect.WPF.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel, IsDesignTimeCreatable=False}"
        Title="Login" Height="500" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Images/login-signupPhoto.jpg" Stretch="UniformToFill" />
        </Grid.Background>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="300" Width="250">
            <Border CornerRadius="20" Background="White">
                <StackPanel Margin="10">
                    <TextBlock Text="Log in" HorizontalAlignment="Center" FontSize="30" FontWeight="Bold"/>
                    <TextBlock Text="Email" Margin="0 10 0 0"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Padding="2"/>
                    <TextBlock Text="Password" Margin="0 10 0 0"/>
                    <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordChanged" Padding="2"/>
                    <TextBlock Text="Email or password is incorrect!" Foreground="Red" Visibility="{Binding NoUserFoundError, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <Grid Margin="0 10">
                        <TextBlock Text="Don't have an account?" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <Button Content="Sign up" Command="{Binding SignupCommand}" HorizontalAlignment="Right" Padding="5"/>
                    </Grid>
                    <Button Content="Log in" Command="{Binding LoginCommand}" Padding="5" FontSize="15" Margin="0 30 0 0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
